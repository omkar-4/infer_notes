## Markdown Block Editor TODO

- Add support for automatic bold and italic - **text** is for bold, _italic_ is for italic
- Add render support for tables. they follow standard markdown syntax
- render text within `text` as monospace code text enclosed within a gray highlight bg box with small border radius.
- NOTE: We will **not** support media for now

## Task

Implement features in the todo list above
