1. Do not overwrite or re-write existing code. Only move around (cut-paste or copy-paste)
2. Never delete existing code, detach obsolete or alternative code from app's business logic and move to lib/debris
3. lib/debris is archieve of isolated discarded debris code
4. Keep logic clean and separated (decoupled) from UI
5. follow a consistent standardized and systematized design pattern throughout the app
6. write and run tests to verify correct functionality of logic code
7. follow a simple, modular architecture.
8. Aim to keep lines of code per file under 100-150 lines.
9. Do not use git commands. It is forbidden to checkout older commits and other branches.
10. Never roll back to previous states of app yourseld using git. Always tell the user to explicitly take git actions when required.
